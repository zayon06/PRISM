import React from 'react';
import ThemeSwitcherWrapper from '../ui/theme-switcher-wrapper';

const NavbarButtonSection = () => {
	return (
		<div className=''>
			<ThemeSwitcherWrapper />
		</div>
	);
};

export default NavbarButtonSection;
